<%
root = function(path){
 return rootPath + path;
};

ogpCheck = function(key){
  var content;
  switch(key){
    case 'title':
      content = (ogp[key]) ? ogp[key] : site.title;
    break;
    case 'type':
      content = (ogp[key]) ? ogp[key] : (path === 'index') ? 'website' : 'article';
    break;
    case 'locale':
      content = (ogp[key]) ? ogp[key] : 'ja_JP';
    break;
    case 'site_name':
      content = (ogp[key]) ? ogp[key] : site.title;
    break;
    case 'description':
      content = (ogp[key]) ? ogp[key] : site.title;
    break;
    case 'url':
      content = (ogp[key]) ? ogp[key] : site.title;
    break;
    case 'image':
      content = (ogp[key]) ? ogp[key] : site.title;
    break;
    default:
      content =  ogp[key];
    break;
  }
  return content;
}
-%>
